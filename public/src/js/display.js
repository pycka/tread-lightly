define(function () {

  var display = {
    canvas: null,

    connect: function (canvasElement) {
      this.canvas = canvasElement;
    },

    draw: function (world) {

    }
  };

  return display;
});